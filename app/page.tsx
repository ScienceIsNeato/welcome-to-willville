/**
 * Town Square (root). All visual content lives in the root layout's
 * <TownStage>; this page exists to provide a static-export entry point.
 */
export default function TownSquare() {
  return null;
}
